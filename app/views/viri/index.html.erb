  <div class="container col-9">
    <% @viri.each do |virus| %>
    <%# <div class="virus-card col-8"> %>
      <div class="virus-content">
        <h2><%= virus.name %></h2>
        <p><%= virus.description %></p>
        <p>$<%= virus.price %></p>
      <div>

      <div class= "d-flex align-items-end mb-2">
        <a class="btn btn-flat" href="#">View</a>
        <a class="btn btn-flat" href="#">Book</a>
      </div>
    <%#</div> %>
  <% end %>
  </div>
